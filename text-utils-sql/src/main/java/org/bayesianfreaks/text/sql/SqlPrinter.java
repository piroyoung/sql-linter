package org.bayesianfreaks.text.sql;

import org.bayesianfreaks.text.parserc.Token;
import org.bayesianfreaks.text.tokenizer.Printer;
import org.bayesianfreaks.text.tokenizer.StateMachine;


public class SqlPrinter implements Printer {
    @Override
    public String write(StateMachine.State state, Token token) {
        return null;
    }
}
